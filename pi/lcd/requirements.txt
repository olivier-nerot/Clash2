# Contrôleur LCD Clash (Raspberry Pi)
Pillow>=10.0.0
websocket-client>=1.6.0

# Matériel (Raspberry Pi uniquement — non requis pour le mode mock/dev) :
st7789>=0.0.4 ; platform_machine == "armv7l" or platform_machine == "aarch64"
gpiozero>=2.0 ; platform_machine == "armv7l" or platform_machine == "aarch64"
